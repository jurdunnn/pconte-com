@props(['title', 'subtitle'])

<div class="flex flex-col gap-y-2 py-12">
    <h3 class="text-center text-yellow-500 text-sm font-light">{{ $title }}</h3>

    <h2 class="text-center text-2xl font-semibold">{{ $subtitle }}</h2>
</div>
